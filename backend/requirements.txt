# Carelink backend runtime dependencies.
# Install:  pip install -r backend/requirements.txt
# (whisper.cpp and Ollama are external local services, not pip packages.)

fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.32
requests==2.34.2
fpdf2==2.8.7
